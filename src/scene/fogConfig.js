/**
 * Fog — manual shader in game and fog test (exp2 + camera distance).
 * FOG_DENSITY ≈ 0.1 → heavy mist by ~18–20 m from the camera.
 */

/** Fog tint — cool dark mist (horizon + clear color). */
export const FOG_COLOR = 0x080b10;

/** Exp2 fog density (manual shader + legacy built-in reference). */
export const FOG_DENSITY = 0.1;
