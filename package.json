{
  "name": "pathway",
  "version": "1.0.0",
  "description": "First-person horror walking game — foggy forest corridor and flashlight",
  "author": "Daniel Gracia",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --mode ghpages",
    "preview": "npm run build:pages && vite preview --mode ghpages",
    "test": "node scripts/run-corridor-tests.mjs && node scripts/run-wolf-encounter-tests.mjs && node scripts/run-conduct-tests.mjs",
    "test:vitest": "vitest run"
  },
  "dependencies": {
    "three": "^0.176.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
